import React from "react";
import { SafeAreaView } from "react-native";

const TodayDetail = ({ weather }) => {


  return (
    <SafeAreaView>
     
    </SafeAreaView>
  );
};

export default TodayDetail;
